// Test setup — intentionally minimal.
// Prisma schema should be pushed to the test database beforehand:
//   pnpm --filter @agenthub/db db:push:test
//
// This is handled by root-level test scripts or CI pipeline.
