export enum ArtifactType {
  Code = "code",
  WebPreview = "web_preview",
  Document = "document",
  Diff = "diff",
}

export enum ArtifactStatus {
  Building = "building",
  Completed = "completed",
  Failed = "failed",
}
